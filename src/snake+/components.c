#include "pch.h"
#include "snake+/components.h"
#include "cnake+/components_definition.h"

#define ALLOC(type, count) (type*)malloc(count*sizeof(type));
#define CHECK(pointer) if(pointer == NULL) 
void snake_initialize_components(){

}

